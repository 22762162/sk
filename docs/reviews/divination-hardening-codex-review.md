# 六爻／梅花候选工程加固：Codex 交叉审查

状态：工程加固审查通过，候选仍为草稿；不构成规则正确性或发布批准。
本分支基于 PR45 的 `5002b6b6466fa332446bb2dc6b27a3167b5ff0b1`，不含 App 接线或运行态提示词。

## 风险分诊

高风险：候选计算入口、合法输入域、适配器接口与输出元数据变化，需要本人完整审阅。
`liuyao.system_cast` 移除是候选接口变化，调用方须迁到 `divination_casting.system_cast_liuyao`。
当前仓库调用点已更新；外部调用方与未来 App 接线不能默默继续使用旧符号。

审查范围内无已知阻断工程缺陷。随机来源移出装卦模块；六爻/梅花对非整数、非法地支和无效
干支配对受控拒绝；原规则表和解释文案未修改。梅花增加两个字段，旧结果字段均保留。

## 证据

- 作者的既有 selftest 与新增 hardening_test 两个脚本由 Codex 复跑通过。
- Codex 增补独立审查脚本 `divination-hardening-check.py`：245,760组六爻有效输入
  （4096种爻值×60日柱，月份轮转覆盖十二支）改前/改后输出相同且未修改输入。
- 27,648组梅花输入（1–48两数×十二时辰）：除明确新增的relation_code/relation_kind，
  旧输出字段逐项相同；新增规则码与既有关系名称一致。
- `make golden-smoke` 102例通过（92例既有排盘双实现对拍，另10例主实现与期望值比对）。
  这不是六爻/梅花独立参考对拍，不扩张其证明范围。
- 红线、生成治理、规则库、提示词对称、预注册及空白检查通过；CI新增执行两个工程自检脚本。

改前/改后比较使用的是同一个主实现的两个版本，并非“盲写对拍”。本会话未读取参考仓源码、
luck.py源码、私有评测、真实数据或密钥；仅通过规定CLI执行既有黄金集并接收聚合结果。

## 保留闸门

1. 候选规则尚无独立参考实现对拍和本人规格签署；不能据工程回归宣称预测准确。
2. 时间起卦尚无经审定农历数据源，不能用公历数字代替；当前梅花仅报数起卦。
3. 未来App入口须限制输入大小、保存起卦方法/原始爻值/时间/时区/换日口径/规则版本，
   保留同题重起关系；本次适配器仅返回方法与爻值，不应冒充完整存档功能。
4. 三术材料分段、三方解释与逐方法反馈评估尚未接入；运行态提示词仍须Fable起草、Codex审计、
   Change Eval及本人批准，不能把新增元数据直接当未来事件判词。
5. 作者状态里“未触L1计算路径”应理解为未改既有Rust排盘；本次候选计算入口仍按高风险处理。
