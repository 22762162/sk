# PR50 成品交叉审查 · Codex · 2026-09-01

审查基线：`2109a75311fdc572d2103c7cd24eaa6aa50a3092`。
范围：三段编排、校验器、作者测试、RFC、调用与存档边界。未读运行态密钥、真实数据、
独立参考实现源码或私有评测集；未运行云模型；未改作者实现/提示词。

结论：**BLOCKED，修复后复审；不能接线或发布。**
作者16项自测通过；独立合成探针12项中仅基础三次调用通过，其余11项未满足验收。
测试源见 `consult-engine/sanshu_cross_review_test.py`，全部使用确定性引擎生成的合成卦快照与假caller。

## 阻断项

1. **封存后又改内容**：`sanshu_orchestrator.py:109/134/150-154`。
   八字/卦段先计算seal并交综合读取，返回前再就地插入event_id，导致返回正文重新hash必不等于seal。
   给事件分配ID必须在封存前完成，或用独立元数据映射且明确保存未变的封存正文。
   合成探针直接验证 `seal(result['bazi']) != result['bazi_seal']`。

2. **受信材料未前置验证**：`:73-108/112-122`。
   `{'unexpected':'not-an-engine-snapshot'}`仍会调用三段；六爻快照搭配method=meihua仍通过；
   空八字材料也生成八字与综合结果。应验证版本、方法、必需字段及引擎重放一致性，
   缺材料按协议产生明确状态，错误快照在任何模型调用前拒绝。

3. **时间/事实边界没有执行**：`:82/94/100`，`sanshu_validator.py:76-84`。
   deadline任意字符串可注入八字材料并传给卦段；未来2099年的known_at/confirmed_at照样入模；
   9月10日截止的问题允许9月30日的事件。输入须结构化日期/时区/冻结as_of，
   已知/确认/读取时间依赖在调用前检查，模型输出窗口限定在本单冻结窗口。
   历史回看需明确不同样本类型；记录字段本身不构成防泄漏。

4. **输入并未冻结**：`:86-97/118-122`。
   caller在第一段返回前修改外部cast_snapshot，后续卦段收到新快照但仍携带旧hash。
   调用开始前防御性复制且冻结全部输入（含prompts/facts_meta）；每个实际发送内容与manifest哈希一致。

5. **失败丢失审计链**：`:59-70/107`。
   caller抛TimeoutError直接逸出；输出连续不合法只抛普通OrchestrationError，已收集attempt/run_id全丢。
   失败回执或异常须携完整脱敏manifest并持久化交给上层，含之前成功段、失败/重试记录；
   传输失败与校验失败分别标注，不能只有成功样本有记录。

6. **综合弃答被标成功**：`:145-149`。
   combined对象合法`status=abstain`后顶层combined_status仍写ok。必须与模型合法状态一致，
   三方完整性、可评分样本和UI不能误把弃答当成有结论。

## 接线前仍缺失的交付（不得以本PR通过替代）

- 文档声称的JSON Schema文件不在PR中；schema版本要对应实际工件，与校验器一致。
- 3/3供应商循环、提供方身份验证、独立slot/adapter/质证/judge、模型配置/提示词/材料hash记录，
  每单预算、超时/取消、完整回执的持久化与回放，尚未形成可验收的完整入口。
- CI没有执行新加的三段测试；请加入独立工程测试任务/现有合适任务，不能只依赖手跑。
- 候选v2、偏袒盲审计、冻结基线Change Eval与所需本人审核尚未完成；不得使用v1或临时占位接线。
- 独立参考测试CI有2失败/176通过（只读取结果摘要）：关系输出缺`subtype`；神煞期望不一致。
  不在本会话改参考源码、不绕过检查，由独立维护流程依据共享契约处理。

以上12项是回归反例，不是独立预测效度评测；不提供“提升准确率”的证据。
