# PR50 成品交叉审查 · Codex · 2026-09-01

最新复核见文末：`033c7b9` 的本次工程阻断已修复，58项合成检查通过；
历史失败记录保留，不代表最新实现仍失败。完整接线、真实评测和发布批准仍不在本次通过范围。

审查基线：`2109a75311fdc572d2103c7cd24eaa6aa50a3092`。
范围：三段编排、校验器、作者测试、RFC、调用与存档边界。未读运行态密钥、真实数据、
独立参考实现源码或私有评测集；未运行云模型；未改作者实现/提示词。

结论：**BLOCKED，修复后复审；不能接线或发布。**
作者16项自测通过；独立合成探针12项中仅基础三次调用通过，其余11项未满足验收。
测试源见 `consult-engine/sanshu_cross_review_test.py`，全部使用确定性引擎生成的合成卦快照与假caller。

## 阻断项

1. **封存后又改内容**：`sanshu_orchestrator.py:109/134/150-154`。
   八字/卦段先计算seal并交综合读取，返回前再就地插入event_id，导致返回正文重新hash必不等于seal。
   给事件分配ID必须在封存前完成，或用独立元数据映射且明确保存未变的封存正文。
   合成探针直接验证 `seal(result['bazi']) != result['bazi_seal']`。

2. **受信材料未前置验证**：`:73-108/112-122`。
   `{'unexpected':'not-an-engine-snapshot'}`仍会调用三段；六爻快照搭配method=meihua仍通过；
   空八字材料也生成八字与综合结果。应验证版本、方法、必需字段及引擎重放一致性，
   缺材料按协议产生明确状态，错误快照在任何模型调用前拒绝。

3. **时间/事实边界没有执行**：`:82/94/100`，`sanshu_validator.py:76-84`。
   deadline任意字符串可注入八字材料并传给卦段；未来2099年的known_at/confirmed_at照样入模；
   9月10日截止的问题允许9月30日的事件。输入须结构化日期/时区/冻结as_of，
   已知/确认/读取时间依赖在调用前检查，模型输出窗口限定在本单冻结窗口。
   历史回看需明确不同样本类型；记录字段本身不构成防泄漏。

4. **输入并未冻结**：`:86-97/118-122`。
   caller在第一段返回前修改外部cast_snapshot，后续卦段收到新快照但仍携带旧hash。
   调用开始前防御性复制且冻结全部输入（含prompts/facts_meta）；每个实际发送内容与manifest哈希一致。

5. **失败丢失审计链**：`:59-70/107`。
   caller抛TimeoutError直接逸出；输出连续不合法只抛普通OrchestrationError，已收集attempt/run_id全丢。
   失败回执或异常须携完整脱敏manifest并持久化交给上层，含之前成功段、失败/重试记录；
   传输失败与校验失败分别标注，不能只有成功样本有记录。

6. **综合弃答被标成功**：`:145-149`。
   combined对象合法`status=abstain`后顶层combined_status仍写ok。必须与模型合法状态一致，
   三方完整性、可评分样本和UI不能误把弃答当成有结论。

## 接线前仍缺失的交付（不得以本PR通过替代）

- 文档声称的JSON Schema文件不在PR中；schema版本要对应实际工件，与校验器一致。
- 3/3供应商循环、提供方身份验证、独立slot/adapter/质证/judge、模型配置/提示词/材料hash记录，
  每单预算、超时/取消、完整回执的持久化与回放，尚未形成可验收的完整入口。
- CI没有执行新加的三段测试；请加入独立工程测试任务/现有合适任务，不能只依赖手跑。
- 候选v2、偏袒盲审计、冻结基线Change Eval与所需本人审核尚未完成；不得使用v1或临时占位接线。
- 独立参考测试CI有2失败/176通过（只读取结果摘要）：关系输出缺`subtype`；神煞期望不一致。
  不在本会话改参考源码、不绕过检查，由独立维护流程依据共享契约处理。

以上12项是回归反例，不是独立预测效度评测；不提供“提升准确率”的证据。

## 既有能力回归与线上预检

在同一独立审查工作区完成：`make test-backend`（46 unittest + 32 bridge pytest）通过，
`make golden-smoke`（102例，其中92双实现比对、10期望值比对）通过；`make redline`、
`make governance-check`、本次attestation检查、`node --check web/app.js`与`web/brain.js`通过。
这些只证明对应范围的既有行为，不覆盖新编排器11个失败反例，也不覆盖参考新增关系/神煞分歧。
现有服务工作目录为主仓，固定域名首页GET返回200；未更换生产进程、数据库或手机App。

## 修复中复测一（作者工作区v2，尚未定版）

原12项反例现全部通过，新增梅花合法输入正例也通过。继续核对原阻断项实际边界，
扩至17项后仍4项失败（测试时钟固定，审计输出隔离到临时目录）：

- 原阻断2尚未完整修复：`_cast_shape_ok`只看版本、卦名、爻数组长度。
  六个null爻的伪快照、篡改六亲字段的引擎快照均被当作受信输入；须按输入重放引擎并比对完整结果，
  不得以浅层shape检查替代原报告要求的重放一致性。
- 原阻断3仍缺窗口起点：`_windows_within`只比较end，start=1900、end=2026-09-30
  仍被认作处在2026-09的冻结窗口内。起止都须受限，冻结时区/目标窗口由调用方明确注入。
- 审计写入失败新增风险：`_persist`捕获OSError后静默pass，即使存储只读也返回成功。
  审计落盘失败必须成为受控失败（异常仍携manifest），或由有确认回执的持久化适配器保证，
  不能仅凭内存结果宣布正式成功。

未降低任何原有验收断言；以上仍是同一批阻断范围的反例扩展，不是发布批准。

## 参考CI防绕过

仅查询远端提交元数据确认参考仓`bfd2c90df6d57c731de0c10b661d750fa9709890`将两项失败
改为xfail，随后主仓CI显示绿。这不是根因修复。主仓参考测试命令增加`--runxfail`，
让参考测试原断言继续作为合入闸门；不读取或修改参考仓实现/测试，不自行裁决差分。
这一保护改动也交Claude交叉审查，原失败仍需独立维护流程处理。

## 定版修复复核（033c7b9）

源提交：`033c7b9a888c838ade55967ec30519064ba7b6f5`。
在作者独立工作区执行三个测试文件，得到 **58 passed**：作者编排测试16、
Codex独立反例/正例17、JSON Schema与校验器一致性25。
另以字段删除、ok附reason、超长dissent_note探针比对schema与校验器，差异列表为空。
原六组阻断以及复测新增的完整快照重放、窗口起点、审计落盘失败问题均已修复。
CI已纳入三段测试并采用参考测试`--runxfail`保护。

参考仓的原独立Codex任务（未接触主实现）汇报：两项自检属于测试错误，
修复并移除xfail，原断言未弱化；`pytest --runxfail -q tests`为178通过、0失败、0跳过。
提交`fff6b8ef468576b895f12aea724a5b3b9fcdc070`，参考仓PR#6待审核合并。
本会话只接收聚合结果和PR元数据，未查看参考源码或测试源码。

结论：**本次已列工程阻断修复通过（范围受限），不是合入、接线或发布批准。**
仍需单独完成：三供应商完整入口与新质证/judge适配、候选v2偏袒审计、契约正式发版、
合规且可复核的真实Change Eval、应有人工裁决及签署。PR51评测器另行审核；
不能用结构测试或100条合成占位材料宣称预测准确率提高。线上服务未改动。
