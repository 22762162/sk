# 红队演练复盘(2026-07-14,dev-plan V2.1 第 11 节第 5 天)

目的:人为注入错误,验证流程闸门逐一能发现。范围为当前已实现的年柱管线。

## 实施记录(三项现场注入,全部被拦截)

| # | 注入 | 模拟的风险 | 结果 |
| --- | --- | --- | --- |
| A | `resolve_bazi_year` 判界 `<` 改 `<=`(恰好立春误归前一年) | 边界舍入实现错误 | ✅ `make golden-smoke` 失败,差分报告落盘;还原后恢复全绿 |
| B | 黄金集塞入毒期望值(1984 恰好立春 → 错误干支「癸亥」) | 规格示例/期望值被写错 | ✅ 40 例中 1 例报警:两实现互相一致但均与期望值冲突——信号正确指向期望值本身,交裁决 |
| C | 锚点公式 `(Y−4)` 篡改为 `(Y−3)` | AI 幻觉写错历法常量 | ✅ 34/39 例瞬间报警 |

差分报告样本存于 CI artifact 约定路径(本地 `golden-tests/reports/`,gitignored)。

## 判读

- **黄金集 + 双实现对拍对"实现错误"和"期望值错误"都有区分度**:实现错→单侧与期望冲突且双实现互不一致;期望错→双实现一致但同时与期望冲突。这正是 INV-08 差分事件分诊所需的信号形状。
- pre-commit 闸门(golden-smoke)会在本地就挡下 A/C 类错误的提交。

## 两项按当前范围顺延的注入(如实记录,不算通过)

| 注入 | 顺延原因 | 触发条件 |
| --- | --- | --- |
| 错误时区 | 引擎当前只收 Unix 秒 + 注入立春,无时区面;时区换算在 backend(zoneinfo)进入后才有攻击面 | V1.0 backend 落地后补演 |
| 评审漏报 | Codex PR 评审尚未接入,当前由本人全审替代 | Codex 接入后:构造含隐蔽缺陷的 PR 验证其分诊召回 |

## 参考实现"偷看"防线核查(过程性,非注入)

- 已落实:参考实现独立仓库、无主仓凭据、会话隔离规则(role-separation.yaml ISO-1/ISO-2)。
- 诚实声明:当前参考实现仍为第一周彩排占位版(同源产出),**不满足异源独立性**;Codex 盲写替换(参考仓 TASK.md)完成前,对拍的"异源信号"价值按一路折算,正确性主要依赖外部 Oracle 与黄金集。

## 结论

年柱范围内协作机制成立:注入的三类错误全部被自动闸门发现,信号形状支持差分分诊。可进入四柱扩展(P1),两项顺延注入随对应模块落地补演。
